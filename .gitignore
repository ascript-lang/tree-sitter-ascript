# Node / node-gyp build artifacts (the committed src/parser.c is the source of truth)
node_modules/
build/
*.node
package-lock.json
# Rust binding build dir
target/
# Standalone grammar crate lock (a tree-sitter grammar is a library crate)
Cargo.lock
